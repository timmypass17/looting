//
//  TopazTests.swift
//  TopazTests
//
//  Created by Timmy Nguyen on 6/19/24.
//

import XCTest

final class CheapSharkAPITests: XCTestCase {
    
//    let service = CheapSharkService()
//    
//    func testFetchingStores() async throws {
//        do {
//            let stores: [Store] = try await service.getStores()
//            XCTAssert(stores.count > 0, "No stores were fetched.")
//        } catch {
//            XCTFail("Fetching stores failed with error: \(error)")
//        }
//    }
//
//    func testDecodingStores() throws {
//        let decoder = JSONDecoder()
//        let stores: [Store] = try decoder.decode([Store].self, from: storesJson)
//        let steamStore = stores.first { $0.name == "Steam" }
//        XCTAssertEqual(steamStore?.name, "Steam")
//    }
//    
//    func testFetchingDeals() async throws {
//        do {
//            let deals: [Deal] = try await service.getDeals()
//            XCTAssert(deals.count > 0, "No deals were fetched.")
//        } catch {
//            XCTFail("Fetching deals failed with error: \(error)")
//        }
//    }
//
//    func testDecodingDeals() throws {
//        let decoder = JSONDecoder()
//        let deals: [Deal] = try decoder.decode([Deal].self, from: dealsJson)
//        let steamID = "238010"
//        let deal = deals.first { $0.steamAppID == steamID }
//        XCTAssertEqual(deal?.title, "Deus Ex: Human Revolution - Director's Cut")
//    }
}
